#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<bool> v(n+1,1); // 배열에 N 개의 index를 채워넣음 true로 초기화
    
    for (int i = 2; i <= n; i++) // 2의 배수를 포함한 범위 내의 배수 지우기 
    {
        if (v[i])
        {
            for (int k = i*i; k <= n; k+=i)
            {
                v[k] = 0;
            }
        }
    }
    
    for (int i = 2; i <= n; i++) //소수인 인덱스만 출력
    {
        if (v[i])
        {
            cout << i << " ";
        }
    }
}
