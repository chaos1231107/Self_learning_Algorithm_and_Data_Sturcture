#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<bool> v(n+1,1);
    
    for (int i = 2; i <= n; i++)
    {
        if (v[i])
        {
            for (int k = i*i; k <= n; k+=i)
            {
                v[k] = 0;
            }
        }
    }
    
    for (int i = 2; i <= n; i++)
    {
        if (v[i])
        {
            cout << i << " ";
        }
    }
}
